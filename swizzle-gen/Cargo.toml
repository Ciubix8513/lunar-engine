[package]
name = "swizzle-gen"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro=true

[dependencies]
syn = { version = "2.0.104", features = [ "derive", "proc-macro", "parsing" ] , default-features = false }
